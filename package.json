{
  "name": "site-motion-capture-mcp-server",
  "version": "1.0.0",
  "description": "MCP bridge for GPU-backed live website motion capture on Vast.ai",
  "type": "module",
  "bin": {
    "site-motion-capture-mcp-server": "index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:coverage": "node --experimental-test-coverage --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
